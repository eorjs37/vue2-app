const str = "hello world";

describe("Testing1", () => {
  test("HelloWorld Component", () => {
    expect(str).toBe("hello world");
  });
  test("HelloWorld Component", () => {
    expect(str).toBe("hello world");
  });
  test("HelloWorld Component", () => {
    expect(str).toBe("hello world");
  });
});
